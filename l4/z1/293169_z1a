#include <iostream>

using namespace std;

int Fib(int n){
	int a = 1, b = 1;
		for (int i = 1; i < n; ++i){
			b = a + b;
			a = b - a;
		}
	return b;
}

int main(){
	int n;
	cin >> n;
	cout << Fib(n);
}
